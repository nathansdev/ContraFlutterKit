import 'dart:ui';

class Settings {
  String title;
  String text;
  bool enable;
  Color bgColor;
  Color textColor;

  Settings({this.title, this.text, this.enable, this.bgColor, this.textColor});
}
